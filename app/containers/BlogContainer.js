const React = require('react')

const BlogContainer = React.createClass({
  render(){
    return (
      <div>
      in BlogContainer
      </div>
    )
  }
})

module.exports = BlogContainer